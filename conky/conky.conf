-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html

conky.config = {
    alignment = 'center_center',
    background = false,
    border_width = 0,
    cpu_avg_samples = 2,
    default_color = 'red',
    default_outline_color = 000000,
    default_shade_color = 000000,
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    gap_x = 1105,
    gap_y = 350,
    minimum_height = 300,
    minimum_width = 350,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_x = true,
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'desktop',
    own_window_transparent = false,
    own_window_argb_visual = true,
    own_window_argb_value = 150,
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 1.0,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
    own_window_hints = 'undecorated,below,skip_taskbar,skip_pager',
    border_inner_margin = 0
}

conky.text = [[

${alignc}${color EAEAEA}${font Open Sans Condensed:pixelsize=150}${time %H:%M}${font}
${alignc}${color ff6600}${font Open Sans Condensed:pixelsize=40}${time %d} ${color EAEAEA}${time %B} ${time %Y}${font}
${alignc}${font Open Sans Condensed:pixelsize=80}${time %A}${font}
]]
